version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd api-blsheet
      - npm install

  build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - api-blsheet/**/*
    - scripts/**/*
    - appspec.yml
