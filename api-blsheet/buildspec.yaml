version: 0.2

phases:
  install:
    commands:
      - cd app-blsheet
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - api-blsheet/**/*
    - scripts/**/*
    - appspec.yml
  exclude-paths:
    - api-blsheet/node_modules/**/*
